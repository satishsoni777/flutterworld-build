import 'package:flutter/material.dart';
import 'package:flutterworld/components/app_appbar.dart';

class FlutterObjectiveScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppAppbar(),
    );
  }
}